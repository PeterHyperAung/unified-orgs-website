export interface OrganizationType {
  name: string;
  slug: string;
  url: string;
  description: string;
  field: string | null;
  goal: string | null;
  base: string | null;
  img: string | null;
}
